package bozoware.client.display.material.impl;

import bozoware.client.display.material.MaterialComponent;

public abstract class MaterialValue extends MaterialComponent {
    public MaterialValue(float componentX, float componentY, float componentWidth, float componentHeight, MaterialComponent parent) {
        super(componentX, componentY, componentWidth, componentHeight, parent);
    }
}
